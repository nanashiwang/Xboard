# 项目协作约定

- 本仓库主分支为 `master`（用户所说的 main 指主分支）。
- 默认直接在主分支完成改动、检查、中文提交并推送；不要自行新建任务分支。
- 只有用户明确要求时才新建分支。已有任务分支的本次改动应合入主分支。
- 提交前执行与改动相关的检查、测试或构建，不混入无关改动。
- 保留已有未提交改动和独有历史，不使用强制推送覆盖远程历史。
- 有现成版本发布流程时沿用该流程，核实构建和发布完成后再报告成功；无实际改动时不创建空提交或版本。
